# Sonora - Standalone Music Player

Sonora is a standalone music player application featuring YouTube Music search, direct URL audio streaming, queue management, and overlay configuration.

## Local Development

1. Install dependencies: `npm install`
2. Run development server: `npm run dev`
3. Open `http://localhost:3000` in your browser.

## Build

```bash
npm run build
```
